<article>
  <header>
    <%= render partial: 'application/errors', locals: { model: @user_review }  %>
  </header>
  <main>
    <%= form_with model: [@user, @user_review] do |user| %>
      <p>
        <span> <%= t 'approve_register' %> </span>
        <%= user.radio_button :status, :approved %>
        <%= user.label :status, 'Sim', value: :approved, checked: true %>
        <%= user.radio_button :status, :refused  %>
        <%= user.label :status, 'Não', value: :refused  %>
      </p>
      <p>
        <%= user.label :refusal %> <%= t 'case_not' %>
        <%= user.text_field :refusal %>
      </p>
      <p>
        <%= user.submit t('buttons.send') %>
      </p>
    <% end %>
  </main>
</article>